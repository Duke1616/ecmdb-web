# 通过该配置兜底解决组件没有类型提示的问题
shamefully-hoist = true

# NOTE: pnpm v9+ 默认屏蔽依赖包构建脚本（postinstall），
# esbuild、vue-demi、core-js-pure 等必须执行构建脚本才能正常工作，
# 此配置确保 Docker 镜像构建时不会因 ERR_PNPM_IGNORED_BUILDS 报错失败
ignore-scripts = false
